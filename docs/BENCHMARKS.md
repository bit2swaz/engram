# Benchmark Documentation

The authoritative benchmark report is the generated file at the repository root: [../BENCHMARKS.md](../BENCHMARKS.md).

That file is produced by `./scripts/generate_benchmark_report.sh` and includes:

- Context assembly latency benchmarks.
- End-to-end throughput and P99 context latency.
- Real-store latency measurements using Redis and LanceDB.

If you need the latest numbers, use the root report instead of this file.
